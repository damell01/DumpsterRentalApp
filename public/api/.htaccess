# Restrict to PHP files only — no directory listing
Options -Indexes

# Only allow access to specific PHP API files
<FilesMatch "^(contact|availability|batch-availability|create-booking|lookup-bookings|push-subscribe|request-portal-link|stripe-webhook)\.php$">
  Require all granted
</FilesMatch>

<FilesMatch "^(?!(contact|availability|batch-availability|create-booking|lookup-bookings|push-subscribe|request-portal-link|stripe-webhook)\.php$)">
  Require all denied
</FilesMatch>
